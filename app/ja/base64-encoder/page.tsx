import { createLocalizedBase64SeoPage } from "@/lib/create-localized-seo-page"

const { metadata, Page } = createLocalizedBase64SeoPage("base64-encoder", "ja")
export { metadata }
export default Page
